# Batch logs

Filled copies of [BATCH_LOG_TEMPLATE.md](../BATCH_LOG_TEMPLATE.md), one per MRE
batch, named `<YYYY-MM-DD>_batch<N>.md`.

PROCESS.md carries the recipe. These carry what was actually done — the difference
between them, across several batches, is the only record of what the process is
actually sensitive to.
